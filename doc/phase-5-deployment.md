# Phase 5: Optimization & Deployment

## Objective
Finalize the site for a professional public launch.

## Tasks
1. **Performance Tuning:**
   - Image optimization via `next/image`.
   - Font optimization via `next/font`.
   - Bundle analysis to ensure minimal JS overhead.
2. **SEO & Metadata:**
   - Configure `metadata` object in `layout.tsx` (title, description, OpenGraph).
   - Add `robots.txt` and `sitemap.xml`.
3. **Deployment:**
   - Connect GitHub repository to Vercel.
   - Configure environment variables.
   - Perform a final lighthouse audit (Target: 90+ in all categories).
4. **Documentation:**
   - Create a final `README.md` for the repository.
   - Recreate these phase plans in the project's `/doc` folder.

## Verification
- Deployed URL is accessible and responsive.
- Meta tags correctly display on social platforms (Twitter/X, LinkedIn).